<Window x:Class="PdfViewerApp.LockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ứng Dụng Bị Khóa" Width="460" Height="280"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#0F172A" Foreground="#F8FAFC"
        FontFamily="Segoe UI, Tahoma, Helvetica"
        WindowStyle="None" AllowsTransparency="True" BorderThickness="1" BorderBrush="#EF4444">
    <Window.Resources>
        <Style x:Key="ModernPasswordBox" TargetType="{x:Type PasswordBox}">
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="BorderBrush" Value="#EF4444" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="PasswordChar" Value="●" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type PasswordBox}">
                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer Name="PART_ContentHost" Margin="0" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="UnlockButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="#FFFFFFFF" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Name="Border" CornerRadius="8" Padding="16,10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#EF4444" Offset="0.0" />
                                    <GradientStop Color="#B91C1C" Offset="1.0" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="#94A3B8" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Name="Border" CornerRadius="8" BorderThickness="1.5" BorderBrush="#334155" Background="#1E293B" Padding="14,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Panel.Background>
            <RadialGradientBrush Center="0.5,0" RadiusX="0.8" RadiusY="0.8">
                <GradientStop Color="#270F0F" Offset="0" />
                <GradientStop Color="#0F172A" Offset="1" />
            </RadialGradientBrush>
        </Panel.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="24,24,24,12">
            <TextBlock Text="CẢNH BÁO BẢO MẬT" FontSize="18" FontWeight="Black" Foreground="#EF4444">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#EF4444" BlurRadius="8" Opacity="0.4" ShadowDepth="0" />
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="Ứng dụng đã bị khóa do phát hiện hành vi dịch ngược hoặc gỡ lỗi trái phép. Vui lòng liên hệ quản trị viên để nhận mã khôi phục mở khóa." 
                       FontSize="12" Foreground="#94A3B8" Margin="0,6,0,0" TextWrapping="Wrap" />
        </StackPanel>
        
        <StackPanel Grid.Row="1" Margin="24,10,24,0">
            <TextBlock Text="MÃ KHÔI PHỤC (RECOVERY KEY)" FontSize="10" FontWeight="Bold" Foreground="#94A3B8" Margin="2,0,0,6" />
            <PasswordBox x:Name="RecoveryKeyBox" Style="{StaticResource ModernPasswordBox}" Height="40" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
        </StackPanel>
        
        <Border Grid.Row="2" Background="#0B0F19" BorderBrush="#1E293B" BorderThickness="0,1,0,0" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Mở khóa ngay" Style="{StaticResource UnlockButton}" Width="150" IsDefault="True" Click="Unlock_Click" />
                <Button Grid.Column="0" Content="Thoát ứng dụng" Style="{StaticResource SecondaryButton}" Width="150" HorizontalAlignment="Left" Click="Exit_Click" />
            </Grid>
        </Border>
    </Grid>
</Window>
