[package]
name = "pdf_core"
version = "1.5.7"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
lopdf = { version = "0.31.0", features = ["embed_image"] }
image = { version = "0.24.7", default-features = false, features = ["jpeg", "png"] }
flate2 = "1.0"
